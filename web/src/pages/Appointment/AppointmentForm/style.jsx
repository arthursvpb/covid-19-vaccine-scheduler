import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  paper: {
    display: 'flex',
    padding: '70px',
    maxWidth: '500px',
  },
  grid: {
    flexDirection: 'row',
  },
});

export default useStyles;
