import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    backgroundColor: '#d2f5e3',
    color: '#234435',
    '&:hover': {
      backgroundColor: '#b1dac5',
      color: '#234435',
    },
  },
});

export default useStyles;
